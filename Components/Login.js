import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Login = () => {
  return (
    <View>
      <Text style={ {fontSize: 25, fontWeight: "bold", marginTop: 50}}>Login</Text>
    </View>
  )
}

export default Login

const styles = StyleSheet.create({})